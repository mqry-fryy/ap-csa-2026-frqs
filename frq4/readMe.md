Question 4: 2D arrays and object methods

My Approach: 
I looped through the row, added all the points values together, and checked if every space had the same color as the first space in the row.

Course Connections:
This connects to nested data, traversing arrays, using booleans, and calling object methods.

What Gave Me Trouble: 
Comparing strings correctly using .equals() instead of ==, and making sure the doubled score only happens when all colors are matched.

What I Would Do Differently: 
Separate the tasks into checking colors and calculating the sum first. 
